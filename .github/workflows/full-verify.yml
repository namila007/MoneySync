name: Full Verify

on:
  pull_request:
    branches:
      - master
  push:
    branches:
      - master

permissions:
  contents: read

jobs:
  hygiene:
    name: Source Hygiene
    uses: ./.github/workflows/reusable-source-hygiene.yml
